This is the repository for the wordpress theme
used by fsmt.dreadworks.de

Dependencies:
	make
	sass
	rsync

Simply run make. There will be a directory ./build where the final wordpress theme is compiled.